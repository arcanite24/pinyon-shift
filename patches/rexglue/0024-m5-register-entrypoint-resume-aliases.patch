diff --git a/resources/templates/codegen/init_cpp.inja b/resources/templates/codegen/init_cpp.inja
index 1be4759..87088d4 100644
--- a/resources/templates/codegen/init_cpp.inja
+++ b/resources/templates/codegen/init_cpp.inja
@@ -31,5 +31,7 @@ const rex::PPCImageInfo PPCImageConfig = {
 
 PPCFuncMapping PPCFuncMappings[] = {
 {% for fn in functions %}{% if not fn.below_code_base or fn.is_import %}	{ {{ fn.address }}, {{ fn.name }} },
+{% for alias in fn.resume_aliases %}	{ {{ alias }}, {{ fn.name }} },
+{% endfor %}
 {% endif %}{% endfor %}	{ 0, nullptr }
 };
diff --git a/tests/unit/codegen/resume_template_test.cpp b/tests/unit/codegen/resume_template_test.cpp
index 84ef561..65429bb 100644
--- a/tests/unit/codegen/resume_template_test.cpp
+++ b/tests/unit/codegen/resume_template_test.cpp
@@ -39,3 +39,38 @@ TEST_CASE("TemplateRegistry: register_cpp maps resumable aliases to their owner"
   CHECK(result.find("SetFunction(0x82010004, sub_82010000)") != std::string::npos);
   CHECK(result.find("SetFunction(0x82010010, sub_82010000)") != std::string::npos);
 }
+
+TEST_CASE("TemplateRegistry: init_cpp maps resumable aliases for the entrypoint image",
+          "[codegen][resume]") {
+  rex::codegen::TemplateRegistry registry;
+  const std::string result = registry.render("codegen/init_cpp", R"({
+    "project": "test_proj",
+    "image_base": "0x82000000",
+    "image_size": "0x1000000",
+    "code_base": "0x82010000",
+    "code_size": "0x100000",
+    "rexcrt_heap": 0,
+    "config_flags": {
+      "skip_lr": false,
+      "ctr_as_local": false,
+      "xer_as_local": false,
+      "reserved_as_local": false,
+      "skip_msr": false,
+      "cr_as_local": false,
+      "non_argument_as_local": false,
+      "non_volatile_as_local": false
+    },
+    "has_dll_modules": false,
+    "is_dll": false,
+    "functions": [{
+      "address": "0x82010000",
+      "name": "sub_82010000",
+      "below_code_base": false,
+      "is_import": false,
+      "resume_aliases": ["0x82010004", "0x82010010"]
+    }]
+  })");
+  CHECK(result.find("{ 0x82010000, sub_82010000 }") != std::string::npos);
+  CHECK(result.find("{ 0x82010004, sub_82010000 }") != std::string::npos);
+  CHECK(result.find("{ 0x82010010, sub_82010000 }") != std::string::npos);
+}
