diff --git a/src/rexglue/commands/migration_scan.cpp b/src/rexglue/commands/migration_scan.cpp
index 5f8e72b488307d383df4cb03a221a95f8db9c67d..6e08863fc41e2aee04edab267627ae6f4cd6e6ba 100644
--- a/src/rexglue/commands/migration_scan.cpp
+++ b/src/rexglue/commands/migration_scan.cpp
@@ -38,6 +38,17 @@ std::string ToLower(std::string s) {
   return s;
 }
 
+std::string NormalizeNewlines(std::string_view input) {
+  std::string normalized;
+  normalized.reserve(input.size());
+  for (std::size_t i = 0; i < input.size(); ++i) {
+    if (input[i] == '\r' && i + 1 < input.size() && input[i + 1] == '\n')
+      continue;
+    normalized.push_back(input[i]);
+  }
+  return normalized;
+}
+
 bool IsSourceExtension(const fs::path& p) {
   static const std::unordered_set<std::string> kExts = {".cpp", ".cc",  ".cxx", ".c",  ".h",
                                                         ".hh",  ".hpp", ".hxx", ".inl"};
@@ -49,12 +60,11 @@ bool IsCmakeFile(const fs::path& p) {
 }
 
 bool IsUnderGeneratedTree(const fs::path& project_root, const fs::path& target) {
-  std::error_code ec;
-  auto rel = fs::relative(target, project_root, ec);
-  if (ec)
+  auto rel = target.lexically_relative(project_root);
+  if (rel.empty())
     return false;
   auto first = rel.begin();
-  return first != rel.end() && first->generic_string().starts_with("generated");
+  return first != rel.end() && *first == "generated";
 }
 
 template <typename Accept, typename Visit>
@@ -84,7 +94,10 @@ void WalkProjectFiles(const fs::path& project_root, Accept accept, Visit visit)
 
 bool IsPathTokenChar(char c) {
   unsigned char uc = static_cast<unsigned char>(c);
-  return std::isalnum(uc) != 0 || c == '_' || c == '-' || c == '.' || c == '/' || c == '\\';
+  // Directory separators delimit a path component and therefore permit a
+  // filename replacement immediately before or after them. Keep punctuation
+  // that may extend the same component (for example, ".bak") as token text.
+  return std::isalnum(uc) != 0 || c == '_' || c == '-' || c == '.';
 }
 
 bool ReplaceAllPathTokens(std::string& haystack, std::string_view needle,
@@ -332,7 +345,7 @@ std::vector<OverwriteEntry> ScanSdkTemplateDrift(const fs::path& project_root,
   fs::path rexglue_cmake = project_root / "generated" / "rexglue.cmake";
   std::string rendered = RenderRexglueCmake(project_name, sdk_version, entrypoint_out_dir);
   std::string on_disk = fs::exists(rexglue_cmake) ? read_file(rexglue_cmake) : std::string{};
-  if (rendered != on_disk) {
+  if (NormalizeNewlines(rendered) != NormalizeNewlines(on_disk)) {
     plan.push_back({rexglue_cmake, std::move(rendered), OverwriteAction::Write, /*silent=*/true,
                     fmt::format("regenerate SDK helper for v{}", sdk_version)});
   }
@@ -370,7 +383,8 @@ std::vector<OverwriteEntry> ScanSourceIncludeRewrites(const fs::path& project_ro
   std::string new_basename = names.snake_case + "_init.h";
   std::string new_basename_lc = ToLower(new_basename);
 
-  static const std::regex include_re(R"(^(\s*#\s*include\s*[<"])([^>"]+)([>"].*)$)");
+  static const std::regex include_re(
+      R"(^(\s*#\s*include\s*[<"])([^>"]+)([>"][^\r\n]*)\r?$)");
   auto extract_target = [&](const std::string& line) -> std::optional<std::string> {
     std::smatch m;
     if (!std::regex_search(line, m, include_re))
diff --git a/tests/unit/codegen/template_registry_test.cpp b/tests/unit/codegen/template_registry_test.cpp
index 89fb6ebeeac3e72a67ca2566caa9f8fd75adad35..32ed1d093949a64e312ab3621a2ff17684b6af71 100644
--- a/tests/unit/codegen/template_registry_test.cpp
+++ b/tests/unit/codegen/template_registry_test.cpp
@@ -65,9 +65,13 @@ TEST_CASE("TemplateRegistry: render with simple CLI data", "[TemplateRegistry]")
   std::string json = R"({
     "names": {"snake_case": "test_app"},
     "sdk_version": "1.0.0",
+    "sdk_version_full": "1.0.0",
+    "generated_on": "2026-07-23T00:00:00Z",
     "include_stamp": false,
     "xex_path": "assets/default.xex",
-    "out_directory_path": "generated/default"
+    "game_root": "",
+    "out_directory_path": "generated/default",
+    "modules": []
   })";
   std::string result = registry.render("init/manifest_toml", json);
 
@@ -85,6 +89,8 @@ TEST_CASE("TemplateRegistry: render with codegen data", "[TemplateRegistry]") {
     "code_base": "0x82010000",
     "code_size": "0x100000",
     "rexcrt_heap": 1,
+    "has_dll_modules": false,
+    "is_dll": false,
     "config_flags": {},
     "functions": [],
     "imports": []
@@ -197,7 +203,7 @@ TEST_CASE("Template: manifest_toml emits sdk_version when include_stamp is true"
           "[TemplateRegistry][manifest]") {
   rex::codegen::TemplateRegistry registry;
   std::string json =
-      R"({"names": {"snake_case": "mygame", "pascal_case": "Mygame", "upper_case": "MYGAME"}, "sdk_version": "0.8.0", "include_stamp": true})";
+      R"({"names": {"snake_case": "mygame", "pascal_case": "Mygame", "upper_case": "MYGAME"}, "sdk_version": "0.8.0", "sdk_version_full": "0.8.0", "generated_on": "2026-07-23T00:00:00Z", "include_stamp": true, "game_root": "", "xex_path": "default.xex", "out_directory_path": "generated/default", "modules": []})";
   std::string out = registry.render("init/manifest_toml", json);
   CHECK(out.find("sdk_version = \"0.8.0\"") != std::string::npos);
   CHECK(out.find("name = \"mygame\"") != std::string::npos);
@@ -207,7 +213,7 @@ TEST_CASE("Template: manifest_toml omits sdk_version when include_stamp is false
           "[TemplateRegistry][manifest]") {
   rex::codegen::TemplateRegistry registry;
   std::string json =
-      R"({"names": {"snake_case": "mygame", "pascal_case": "Mygame", "upper_case": "MYGAME"}, "sdk_version": "0.8.0", "include_stamp": false})";
+      R"({"names": {"snake_case": "mygame", "pascal_case": "Mygame", "upper_case": "MYGAME"}, "sdk_version": "0.8.0", "sdk_version_full": "0.8.0", "generated_on": "2026-07-23T00:00:00Z", "include_stamp": false, "game_root": "", "xex_path": "default.xex", "out_directory_path": "generated/default", "modules": []})";
   std::string out = registry.render("init/manifest_toml", json);
   CHECK(out.find("sdk_version") == std::string::npos);
   CHECK(out.find("name = \"mygame\"") != std::string::npos);
