diff --git a/include/rex/codegen/function_node.h b/include/rex/codegen/function_node.h
index a335b37eb8e1aab88dafa996e557a0af6339a6cb..b6f7b2bc3fe25041ec64ccde42620b66362002df 100644
--- a/include/rex/codegen/function_node.h
+++ b/include/rex/codegen/function_node.h
@@ -98,6 +98,10 @@ class FunctionNode {
   /// For normal functions: emits REX_FUNC with blocks and instructions
   std::string emitCpp(const EmitContext& ctx) const;
 
+  /// Return PCs immediately following linked branches that can safely resume
+  /// inside this function's emitted blocks.
+  std::vector<uint32_t> resumableReturnAddresses(const BinaryView& binary) const;
+
   //=========================================================================
   // Instruction access (valid after discover)
   //=========================================================================
diff --git a/include/rex/ppc/context.h b/include/rex/ppc/context.h
index 2d571e3d5b75b9f4a0ea54c37a56a89b59a4c0e8..51e85e08db56e2a9681227f9fb96e517e03152a2 100644
--- a/include/rex/ppc/context.h
+++ b/include/rex/ppc/context.h
@@ -306,6 +306,13 @@ struct alignas(0x40) PPCContext {
    */
   uint32_t last_indirect_target = 0;
 
+  /**
+   * Guest address requested by table-based dispatch. Generated functions
+   * consume this on entry so a saved LR that points immediately after a
+   * linked call can resume inside its owning AOT function.
+   */
+  uint32_t dispatch_address = 0;
+
   PPCRegister f0;
   PPCRegister f1;
   PPCRegister f2;
diff --git a/resources/templates/codegen/_indirect_call.inja b/resources/templates/codegen/_indirect_call.inja
index d4b1830b277014fec9c9395a091b779d20298abd..54efdd437d420f158209bcd4dd36dd6e5caee93e 100644
--- a/resources/templates/codegen/_indirect_call.inja
+++ b/resources/templates/codegen/_indirect_call.inja
@@ -34,5 +34,7 @@
       ctx.last_indirect_target = rex_indirect_target_;                                     \
       rex_indirect_func_ = rex::runtime::ResolveIndirectFunction(rex_indirect_target_);    \
     }                                                                                      \
+    ctx.dispatch_address = rex_indirect_target_;                                           \
     rex_indirect_func_(ctx, base);                                                         \
+    ctx.dispatch_address = 0;                                                              \
   } while (0)
diff --git a/resources/templates/codegen/register_cpp.inja b/resources/templates/codegen/register_cpp.inja
index 0be31c412f32f30dac0b0622da00189f084c98b0..9c8fc7cb31c2e4a7b01ee3157ea7219fc441154c 100644
--- a/resources/templates/codegen/register_cpp.inja
+++ b/resources/templates/codegen/register_cpp.inja
@@ -18,6 +18,8 @@ void ReXModule_Register(rex::runtime::IModuleRegistrar* registrar) {
 void {{ project }}_RegisterFunctions(rex::runtime::IModuleRegistrar* registrar) {
 {% endif %}
 {% for fn in functions %}{% if not fn.below_code_base or fn.is_import %}  registrar->SetFunction({{ fn.address }}, {{ fn.name }});
+{% for alias in fn.resume_aliases %}  registrar->SetFunction({{ alias }}, {{ fn.name }});
+{% endfor %}
 {% endif %}{% endfor %}}
 
 {% if is_dll %}
diff --git a/src/codegen/codegen_writer.cpp b/src/codegen/codegen_writer.cpp
index 44efad4c9226f592061085c28e878fb843df6e20..28ff3daf389839fc374bb08f2715ca2ff0be5d4b 100644
--- a/src/codegen/codegen_writer.cpp
+++ b/src/codegen/codegen_writer.cpp
@@ -67,12 +67,23 @@ nlohmann::json buildTemplateData(const rex::codegen::CodegenContext& ctx,
       funcName = fmt::format("sub_{:08X}", fn->base());
     }
 
+    nlohmann::json resumeAliases = nlohmann::json::array();
+    if (fn->authority() != rex::codegen::FunctionAuthority::IMPORT) {
+      for (uint32_t address : fn->resumableReturnAddresses(ctx.binary())) {
+        const auto* explicitEntry = ctx.graph.getFunction(address);
+        if (!explicitEntry || explicitEntry == fn) {
+          resumeAliases.push_back(fmt::format("0x{:X}", address));
+        }
+      }
+    }
+
     functionsJson.push_back({
         {"address", fmt::format("0x{:X}", fn->base())},
         {"name", funcName},
         {"is_rexcrt", isRexcrt},
         {"below_code_base", (fn->base() < codeMin)},
         {"is_import", fn->authority() == rex::codegen::FunctionAuthority::IMPORT},
+        {"resume_aliases", std::move(resumeAliases)},
     });
   }
 
diff --git a/src/codegen/function_graph.cpp b/src/codegen/function_graph.cpp
index 04510193f82e095c008f2ce5a13a8622af728db7..441b757e9851b4b4b37613906e6b27bfda79f23b 100644
--- a/src/codegen/function_graph.cpp
+++ b/src/codegen/function_graph.cpp
@@ -346,6 +346,33 @@ void emit_print(std::string& out, fmt::format_string<Args...> fmt, Args&&... arg
 
 }  // namespace
 
+std::vector<uint32_t> FunctionNode::resumableReturnAddresses(const BinaryView& binary) const {
+  std::vector<uint32_t> addresses;
+
+  for (const auto& block : blocks()) {
+    const auto* blockData = binary.translate(block.base);
+    if (!blockData) {
+      continue;
+    }
+
+    for (uint32_t address = block.base; address < block.end(); address += 4) {
+      const uint32_t instruction = load_and_swap<uint32_t>(blockData + address - block.base);
+      if (!PPC_BL(instruction)) {
+        continue;
+      }
+
+      const uint32_t returnAddress = address + 4;
+      if (containsAddress(returnAddress)) {
+        addresses.push_back(returnAddress);
+      }
+    }
+  }
+
+  std::sort(addresses.begin(), addresses.end());
+  addresses.erase(std::unique(addresses.begin(), addresses.end()), addresses.end());
+  return addresses;
+}
+
 std::string FunctionNode::emitCpp(const EmitContext& ctx) const {
   if (authority() == FunctionAuthority::IMPORT) {
     return "";
@@ -385,6 +412,8 @@ std::string FunctionNode::emitCpp(const EmitContext& ctx) const {
   // --- First pass: collect labels from all blocks ---
   std::unordered_set<size_t> labels;
   labels.reserve(64);
+  const auto resumeAddresses = resumableReturnAddresses(ctx.binary);
+  labels.insert(resumeAddresses.begin(), resumeAddresses.end());
 
   for (const auto& block : blocks()) {
     auto* blockData = reinterpret_cast<const uint32_t*>(ctx.binary.translate(block.base));
@@ -494,6 +523,21 @@ std::string FunctionNode::emitCpp(const EmitContext& ctx) const {
   std::string body;
   body.reserve(4096);
 
+  if (!resumeAddresses.empty()) {
+    emit_println(body, "\tconst uint32_t rex_dispatch_address = ctx.dispatch_address;");
+    emit_println(body, "\tctx.dispatch_address = 0;");
+    emit_println(body, "\tswitch (rex_dispatch_address) {{");
+    emit_println(body, "\t\tcase 0:");
+    emit_println(body, "\t\tcase 0x{:08X}: break;", base());
+    for (uint32_t address : resumeAddresses) {
+      emit_println(body, "\t\tcase 0x{:08X}: goto loc_{:X};", address, address);
+    }
+    emit_println(body, "\t\tdefault: break;");
+    emit_println(body, "\t}}\n");
+  } else {
+    emit_println(body, "\tctx.dispatch_address = 0;");
+  }
+
   ppc_insn insn;
   std::unordered_set<size_t> emittedLabels;
 
diff --git a/src/core/CMakeLists.txt b/src/core/CMakeLists.txt
index faa4847b2cccd78160acc1ac2a93615955303233..2cc6305f22d7542672ede2bc24a97e774b38e22f 100644
--- a/src/core/CMakeLists.txt
+++ b/src/core/CMakeLists.txt
@@ -88,7 +88,9 @@ target_include_directories(rexcore PRIVATE
     ${PROJECT_SOURCE_DIR}/thirdparty/cli11/include
 )
 
-if(UNIX)
+if(WIN32)
+    target_link_libraries(rexcore PUBLIC ws2_32)
+elseif(UNIX)
     target_link_libraries(rexcore PRIVATE pthread rt dl)
 endif()
 
@@ -122,4 +124,3 @@ if(REXGLUE_ENABLE_PERF_COUNTERS)
     target_compile_definitions(rexcore PUBLIC
         $<$<NOT:$<CONFIG:Release>>:REXGLUE_ENABLE_PERF_COUNTERS>)
 endif()
-
diff --git a/src/system/function_dispatcher.cpp b/src/system/function_dispatcher.cpp
index d22f57cc0c55eb3e866972e19f39665320200e25..5d0fb10e59267a88039e4869989490da4848f38d 100644
--- a/src/system/function_dispatcher.cpp
+++ b/src/system/function_dispatcher.cpp
@@ -82,7 +82,9 @@ bool FunctionDispatcher::Execute(ThreadState* thread_state, uint32_t address) {
   uint64_t previous_lr = ctx->lr;
   ctx->lr = 0xBCBCBCBC;
 
+  ctx->dispatch_address = address;
   fn(*ctx, memory_->virtual_membase());
+  ctx->dispatch_address = 0;
 
   ctx->lr = previous_lr;
   ctx->r1.u64 += 64 + 112;
diff --git a/src/system/xthread.cpp b/src/system/xthread.cpp
index 54b4d9c0ce9156e7f882e31c1612edfe3553169f..6b3fd18eafd3409fd5045ea2c96a66a7a6fe7d1f 100644
--- a/src/system/xthread.cpp
+++ b/src/system/xthread.cpp
@@ -776,7 +776,9 @@ void XThread::Execute() {
     }
 
     REXSYS_NOISY_DEBUG("XThread::Execute - Calling function at {:08X}", dispatch_address);
-    func(*ctx, base);
+      ctx->dispatch_address = dispatch_address;
+      func(*ctx, base);
+      ctx->dispatch_address = 0;
     exit_code = static_cast<int>(ctx->r3.u32);
 
     if (continue_reentry) {
diff --git a/tests/unit/CMakeLists.txt b/tests/unit/CMakeLists.txt
index 16c8a53e0b63fb1c924a672875e121a38d5f2270..095c3411cdcbd84e209368cf3789e2901695cb78 100644
--- a/tests/unit/CMakeLists.txt
+++ b/tests/unit/CMakeLists.txt
@@ -10,6 +10,7 @@ add_executable(unit_tests
     core/byte_order_test.cpp
     core/chrono_test.cpp
     codegen/config_test.cpp
+    codegen/resume_entry_test.cpp
     codegen/template_registry_test.cpp
     ppc/import_function_test.cpp
     ppc/stack_test.cpp
diff --git a/tests/unit/codegen/resume_entry_test.cpp b/tests/unit/codegen/resume_entry_test.cpp
new file mode 100644
index 0000000000000000000000000000000000000000..bfb4eaba15f82339f188f4efa05f8c3f86385b62
--- /dev/null
+++ b/tests/unit/codegen/resume_entry_test.cpp
@@ -0,0 +1,46 @@
+/**
+ * @file        tests/unit/codegen/resume_entry_test.cpp
+ * @brief       Synthetic coverage for interior-PC AOT resume entries
+ */
+
+#include <array>
+#include <map>
+
+#include <catch2/catch_test_macros.hpp>
+
+#include <rex/codegen/binary_view.h>
+#include <rex/codegen/codegen_context.h>
+#include <rex/codegen/config.h>
+#include <rex/codegen/function_graph.h>
+#include <rex/codegen/test_support.h>
+
+TEST_CASE("FunctionNode: linked-call return PC emits an interior resume entry",
+          "[codegen][resume]") {
+  constexpr uint32_t kBase = 0x82010000u;
+  std::array<uint32_t, 3> words = {
+      __builtin_bswap32(0x48000009u),  // bl kBase + 8
+      __builtin_bswap32(0x60000000u),  // nop (saved LR resumes here)
+      __builtin_bswap32(0x4E800020u),  // blr
+  };
+
+  rex::codegen::TestModule module;
+  module.Load(kBase, reinterpret_cast<const uint8_t*>(words.data()), sizeof(words));
+  auto binary = rex::codegen::BinaryView::fromModule(module);
+  rex::codegen::RecompilerConfig config;
+  auto ctx = rex::codegen::CodegenContext::Create(std::move(binary), std::move(config));
+
+  rex::codegen::AnalyzeTestBinary(
+      ctx, "resume", {{kBase, "test_owner"}, {kBase + 8, "test_callee"}}, kBase,
+      reinterpret_cast<const uint8_t*>(words.data()), sizeof(words));
+
+  const auto* owner = ctx.graph.getFunction(kBase);
+  REQUIRE(owner != nullptr);
+  CHECK(owner->resumableReturnAddresses(ctx.binary()) == std::vector<uint32_t>{kBase + 4});
+
+  rex::codegen::EmitContext emit{
+      ctx.binary(), ctx.Config(), ctx.graph, kBase + 8, nullptr};
+  const std::string output = owner->emitCpp(emit);
+  CHECK(output.find("case 0x82010004: goto loc_82010004;") != std::string::npos);
+  CHECK(output.find("loc_82010004:") != std::string::npos);
+  CHECK(output.find("ctx.dispatch_address = 0;") != std::string::npos);
+}
diff --git a/tests/unit/codegen/template_registry_test.cpp b/tests/unit/codegen/template_registry_test.cpp
index 32ed1d093949a64e312ab3621a2ff17684b6af71..7ea4a4b8056e016a1e6af05e5e62ac8dabd10283 100644
--- a/tests/unit/codegen/template_registry_test.cpp
+++ b/tests/unit/codegen/template_registry_test.cpp
@@ -160,6 +160,41 @@ TEST_CASE("TemplateRegistry: ppc_config_h includes shared indirect-call partial"
   CHECK(result.find("[[likely]]") != std::string::npos);
 }
 
+TEST_CASE("TemplateRegistry: indirect dispatch carries and clears the requested guest address",
+          "[TemplateRegistry][resume]") {
+  rex::codegen::TemplateRegistry registry;
+  std::string json = R"({
+    "image_base": "0x82000000",
+    "image_size": "0x1000000",
+    "code_base": "0x82010000",
+    "code_size": "0x100000",
+    "thunk_reserve_size": "0x1000"
+  })";
+  std::string result = registry.render("codegen/_indirect_call", json);
+  CHECK(result.find("ctx.dispatch_address = rex_indirect_target_") != std::string::npos);
+  CHECK(result.find("ctx.dispatch_address = 0") != std::string::npos);
+}
+
+TEST_CASE("TemplateRegistry: register_cpp maps resumable return aliases to their owner",
+          "[TemplateRegistry][resume]") {
+  rex::codegen::TemplateRegistry registry;
+  std::string json = R"({
+    "project": "test_proj",
+    "is_dll": false,
+    "functions": [{
+      "address": "0x82010000",
+      "name": "sub_82010000",
+      "below_code_base": false,
+      "is_import": false,
+      "resume_aliases": ["0x82010004", "0x82010010"]
+    }]
+  })";
+  std::string result = registry.render("codegen/register_cpp", json);
+  CHECK(result.find("SetFunction(0x82010000, sub_82010000)") != std::string::npos);
+  CHECK(result.find("SetFunction(0x82010004, sub_82010000)") != std::string::npos);
+  CHECK(result.find("SetFunction(0x82010010, sub_82010000)") != std::string::npos);
+}
+
 TEST_CASE("TemplateRegistry: cmake_var callback works", "[TemplateRegistry]") {
   rex::codegen::TemplateRegistry registry;
   std::string result = registry.renderString("{{ cmake_var(\"FOO\") }}", "{}");
diff --git a/tests/unit/system/function_dispatcher_test.cpp b/tests/unit/system/function_dispatcher_test.cpp
index 7721c40033a4f29c4316c1dcadb628e45ed64572..608d7c88da6a6c6f7c0cac89f967911a98949318 100644
--- a/tests/unit/system/function_dispatcher_test.cpp
+++ b/tests/unit/system/function_dispatcher_test.cpp
@@ -15,6 +15,7 @@
 #include <rex/ppc/context.h>
 #include <rex/system/export_resolver.h>
 #include <rex/system/function_dispatcher.h>
+#include <rex/system/thread_state.h>
 #include <rex/system/xmemory.h>
 
 namespace {
@@ -32,6 +33,11 @@ rex::memory::Memory& GetTestMemory() {
 
 void DummyFn(PPCContext&, uint8_t*) {}
 void OtherDummyFn(PPCContext&, uint8_t*) {}
+uint32_t g_observed_dispatch_address = 0;
+void ObserveDispatchFn(PPCContext& ctx, uint8_t*) {
+  g_observed_dispatch_address = ctx.dispatch_address;
+  ctx.dispatch_address = 0;
+}
 
 }  // namespace
 
@@ -202,3 +208,23 @@ TEST_CASE("FunctionDispatcher: UnregisterModule on unknown id returns nullopt",
   rex::runtime::FunctionDispatcher dispatcher(&memory, &resolver);
   CHECK_FALSE(dispatcher.UnregisterModule("nope").has_value());
 }
+
+TEST_CASE("FunctionDispatcher: Execute carries the requested guest entry address",
+          "[runtime][dispatcher][resume]") {
+  auto& memory = GetTestMemory();
+  rex::runtime::ExportResolver resolver;
+  rex::runtime::FunctionDispatcher dispatcher(&memory, &resolver);
+
+  constexpr uint32_t kModuleBase = 0x89000000u;
+  constexpr uint32_t kCodeSize = 0x10000u;
+  constexpr uint32_t kImageSize = 0x100000u;
+  constexpr uint32_t kResumeAddress = kModuleBase + 0x104u;
+  REQUIRE(dispatcher.InitializeFunctionTable(kModuleBase, kCodeSize, kModuleBase, kImageSize));
+  dispatcher.SetFunction(kResumeAddress, &ObserveDispatchFn);
+
+  rex::runtime::ThreadState thread_state(1, 0x10000u, 0, &memory);
+  g_observed_dispatch_address = 0;
+  dispatcher.Execute(&thread_state, kResumeAddress, nullptr, 0);
+  CHECK(g_observed_dispatch_address == kResumeAddress);
+  CHECK(thread_state.context()->dispatch_address == 0);
+}
