diff --git a/include/rex/system/xthread.h b/include/rex/system/xthread.h
index 94135af..c643d0b 100644
--- a/include/rex/system/xthread.h
+++ b/include/rex/system/xthread.h
@@ -32,6 +32,10 @@ namespace rex::system {
 
 constexpr memory::fourcc_t kThreadSaveSignature = memory::make_fourcc("THRD");
 
+// High-volume restored-continuation diagnostics are opt-in so ordinary
+// gameplay does not synchronously write thousands of trace lines.
+bool IsReentryTraceEnabled();
+
 class XEvent;
 
 enum IRQL_FLAGS : uint8_t {
diff --git a/src/kernel/xboxkrnl/xboxkrnl_threading.cpp b/src/kernel/xboxkrnl/xboxkrnl_threading.cpp
index 22fe01f..64553f1 100644
--- a/src/kernel/xboxkrnl/xboxkrnl_threading.cpp
+++ b/src/kernel/xboxkrnl/xboxkrnl_threading.cpp
@@ -278,7 +278,8 @@ void KeSetCurrentStackPointers_entry(mapped_void stack_ptr, ppc_ptr_t<X_KTHREAD>
       REX_KERNEL_MEMORY()->TranslateVirtual<X_KPCR*>(static_cast<uint32_t>(context->r13.u64));
 
   bool expected = false;
-  if (g_m3_logged_current_stack_pointers.compare_exchange_strong(expected, true)) {
+  if (IsReentryTraceEnabled() &&
+      g_m3_logged_current_stack_pointers.compare_exchange_strong(expected, true)) {
     REXKRNL_INFO(
         "M3_TRACE stack.reentry.first lr={:08X} thread={:08X} current_thread={:08X} "
         "fiber={:08X} sp={:08X} alloc={:08X} base={:08X} limit={:08X}",
diff --git a/src/system/xthread.cpp b/src/system/xthread.cpp
index 6b3fd18..24e7455 100644
--- a/src/system/xthread.cpp
+++ b/src/system/xthread.cpp
@@ -47,6 +47,21 @@ REXCVAR_DEFINE_BOOL(ignore_thread_affinities, true, "Kernel",
 
 namespace rex::system {
 
+bool IsReentryTraceEnabled() {
+  static const bool enabled = [] {
+    const char* value = std::getenv("PINYON_SHIFT_REENTRY_TRACE");
+    return value && std::strcmp(value, "1") == 0;
+  }();
+  if (enabled) {
+    static const bool activation_logged = [] {
+      REXSYS_INFO("M4_TRACE reentry.tracing enabled=1");
+      return true;
+    }();
+    (void)activation_logged;
+  }
+  return enabled;
+}
+
 const uint32_t XAPC::kSize;
 const uint32_t XAPC::kDummyKernelRoutine;
 const uint32_t XAPC::kDummyRundownRoutine;
@@ -672,6 +687,9 @@ void XThread::Execute() {
   REXSYS_NOISY_DEBUG("Execute thid {} (handle={:08X}, '{}', native={:08X})", thread_id_, handle(),
                      thread_name_, thread_->system_id());
 
+  // Emit the opt-in activation marker even before the first restored continuation.
+  (void)IsReentryTraceEnabled();
+
   // Let the kernel know we are starting.
   kernel_state_->OnThreadExecute(this);
 
@@ -748,7 +766,9 @@ void XThread::Execute() {
   if (setjmp(reentry_jmp_buf_) != 0) {
     continue_reentry = true;
     next_address = reentry_address_;
-    LogMissingReentryStackContinuations(memory, dispatcher, ctx, guest_object<X_KTHREAD>());
+    if (IsReentryTraceEnabled()) {
+      LogMissingReentryStackContinuations(memory, dispatcher, ctx, guest_object<X_KTHREAD>());
+    }
   }
 
   while (next_address != 0) {
@@ -763,7 +783,7 @@ void XThread::Execute() {
 
     // Emit before entering AOT code: a later context restore may abandon this
     // host call chain, in which case the post-call event is never reached.
-    if (continue_reentry) {
+    if (continue_reentry && IsReentryTraceEnabled()) {
       const uint64_t sequence =
           g_m4_reentry_dispatch_sequence.fetch_add(1, std::memory_order_relaxed) + 1;
       REXSYS_INFO(
@@ -782,8 +802,10 @@ void XThread::Execute() {
     exit_code = static_cast<int>(ctx->r3.u32);
 
     if (continue_reentry) {
-      REXSYS_INFO("M3_TRACE stack.reentry.return entry={:08X} lr={:08X}", dispatch_address,
-                  static_cast<uint32_t>(ctx->lr));
+      if (IsReentryTraceEnabled()) {
+        REXSYS_INFO("M3_TRACE stack.reentry.return entry={:08X} lr={:08X}", dispatch_address,
+                    static_cast<uint32_t>(ctx->lr));
+      }
       next_address = static_cast<uint32_t>(ctx->lr);
     }
   }
@@ -794,7 +816,9 @@ void XThread::Execute() {
 }
 
 void XThread::Reenter(uint32_t address) {
-  REXSYS_INFO("M3_TRACE stack.reentry.transfer address={:08X}", address);
+  if (IsReentryTraceEnabled()) {
+    REXSYS_INFO("M3_TRACE stack.reentry.transfer address={:08X}", address);
+  }
   reentry_address_ = address;
   std::longjmp(reentry_jmp_buf_, 1);
 }
